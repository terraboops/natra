# Binaries
bin/
*.o
*.a
# Stray `go build ./cmd/vm-rig` lands the binary at the repo root.
/vm-rig

# eBPF
bpf/*.o
bpf/headers/vmlinux.h

# Go
vendor/
*.test
*.out
coverage.txt
*.prof

# Kubebuilder
testbin/
cover.out

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Temp
tmp/
*.tmp

# Build artifacts
dist/
*.tar.gz
*.zip
.claude/

# Profiling / soak / measurement data — never check in
results/
*.pprof
heap-*
snapshots-*.jsonl

# Local working notes from the superpowers skill (specs/plans/etc).
# Stays on disk for reference; never tracked.
docs/superpowers/
