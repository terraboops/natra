{
  "kernel": "local",
  "_comment": "Local Docker (LinuxKit / colima) baseline. Generous ceiling; real measurements on M-series are 700-800 ns/op against natra.bpf.o. Refresh with `make perf-baseline KERNEL=local`.",
  "bpf_prog_run_ns_per_op_max": 5000.0
}
